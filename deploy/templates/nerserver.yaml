apiVersion: v1
kind: Pod
metadata:
  labels:
    app_name: nerserver
  name: nerserver
  namespace: default
spec:
  containers:
  - image: nerserver:latest
    imagePullPolicy: IfNotPresent
    name: nerserver
    args:
    - -f
    - /dev/null
    command:
    - tail
    ports:
    - containerPort: 5000
      protocol: TCP
    volumeMounts:
    - mountPath: /opt/app/nerserver
      name: data
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: shared-pvc

